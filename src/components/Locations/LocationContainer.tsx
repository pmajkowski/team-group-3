import React from "react";
import classes from "./LocationContainer.module.css";

const LocationContainer = (): JSX.Element => {
  return <div className={classes.container}>LocationPoints</div>;
};

export default LocationContainer;
